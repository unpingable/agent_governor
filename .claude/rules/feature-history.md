# Feature History

Detailed design notes for shipped features. Not auto-loaded — read on demand when working on or near a specific subsystem. The terse orientation index is `implementation-summary.md`.

## Lifecycle structure (2026-05-26)

Per `specs/gaps/GOV_GAP_FEATURE_HISTORY_LIFECYCLE_001.md` the file is organized by trajectory:

- **`## Live`** — entries currently part of the platform / research / substrate set, subgrouped by bin (`3.x platform` / `current research` / `candidate substrate`).
- **`## Retired`** — entries no longer part of the live orientation set. Retirement is an *orientation signal*, not a deletion instruction (per §7.2 of the gap). Retired entries carry a closed-vocabulary reason prefix.

Bin definitions are in §5 and §5.1 of the gap doc. Within each Live subgroup, entries appear in original chronological order.

## Live

### 3.x platform

Items that directly gate, transition, constrain, authorize, recover, supervise, or emit receipts at the daemon / RPC / hook / runtime layer.

**Phase 1-3 COMPLETE** — All 14 steps from BUILD_SPEC.md.
**Multi-Agent v2** — SQLite backend, leases, epochs, permissions, dispatcher protocol.
**Epistemic Governance** — Provenance tracking, confidence modeling, dangerous claim detection.
**Regime Detection** — Operational health monitoring (ELASTIC/WARM/DUCTILE/UNSTABLE).
**Boil Control** — Named presets (GREEN_TEA → BOIL), dwell time enforcement, tripwires.
**Jurisdictions** — Context-aware governance (FACTUAL, SPECULATIVE, ADVERSARIAL, etc.).
**Security Verifier** — Secret detection, SQL/command injection, XSS, path traversal.
**Watch Mode** — Continuous file monitoring with automatic security scanning.
**Claude Code Hooks** — Integration with Claude CLI via pre/post tool hooks.
**Fiction Governor** — Plot threads, scene proposals, prompt generation, narrative constraints, manuscript scanning, similarity matching, context drift detection, fiction guardrails (consent tracking, DSI, AII).
**Non-Fiction Governor** — Corpus management, DOI fetching, citation verification, CFI v0 (contextual frame intrusion detection, 12-frame taxonomy, perspective tracking, normative creep, scope violations).
**Multi-Agent Routing** — Task complexity estimation, model tiers, adaptive routing.
**Failure Provenance** — Scars (constraint hysteresis), shields (input gating), surprise ratio classification. Scar fingerprints: `failure_kind` + `action_type` for action-level novelty gating, separate evidence lifecycles per failure mode.
**Grounding Audit** — Closed-loop hallucination detection, failure mode taxonomy, adaptive policy thresholds.
**Ultrastability** — Ashby-style S₁ adaptation, bounded parameters, pathology detection, freeze/unfreeze.
**Homeostat** — Exploration budgets, adaptive gain scheduling, domain-specific setpoints, 7 exploration contexts.
**Coupling** — Homeostat→Ultrastability one-way protocol, TuningIntent gate, S₁ bounds enforcement, deadband, accumulator, freeze feedback.
**Strict Mode** — Fail-closed governance preset, claim categories, risk-adjusted requirements, commit levels.
**Drift Detection** — Temporal asymmetry defense, premise quarantine, attention skew, coherence gradient.
**Ops Governor** — Runbook verification, time window enforcement, blast radius limits, precondition chains.
**Config Profiles** — Named governance presets (strict, permissive, research, production, audit, research_mode), custom profiles, one-command switching.
**Dissent Ledger** — Contradiction persistence, first-class objections, commit gating, confidence trajectories.
**TTL Enforcement** — Recency decay, volatility classes (PERMANENT→EPHEMERAL), revalidation scheduling.
**Quorum State Machine** — Multi-agent consensus protocol, Δt stability windows, claim-type policies, dissent/TTL integration, risk levels, fingerprint gating, escalation/resolution states.
**Cooperative Redundancy** — Independence scoring, method signatures, Jaccard similarity, anti-cheat (source URL overlap), quorum integration.
**Semantic Variety** — Post-commit text transform, phrase bank with meaning tags, cooldown tracking, semantic diff guard, no-rewrite zones, burst repetition detection.
**Context Drift Detection** — Narrative mode tracking with hysteresis, genre escalation gating, register shift detection, mode chatter warnings.
**Fiction Guardrails** — Consent tracking (pairwise, scoped), DSI detection, AII with validity profiles, hard constraints (C1-C3), soft penalties (P1-P4).
**Sybil Resistance** — Bloc detection, effective voter count (Neff), per-origin budget coupling, quorum Gate 5 integration, escalation triggers.
**Research Mode** — Non-convergent epistemic control, hypothesis lifecycle (PROBE→TENTATIVE→SUPPORTED→ABANDONED), entropy bounds, dominance caps, evidence impulses with decay, Δt invariant, terminal states.
**ClaimStatus + Epistemic Persistence** — ClaimStatus enum (PROPOSED→SUPPORTED→CONTESTED→…), QuorumStatus mapping, EpistemicLedger SQLite persistence (Schema V4), write-through on mutations, from_dict/from_json deserialization.
**Evidence Type Validation** — Layer 3 evidence kind gating, 4 new EvidenceType values (CALC_RESULT, TEST_RESULT, WEB_SOURCE, LIVE_RETRIEVAL), WRONG_EVIDENCE_TYPE audit failure mode, required_evidence_kinds on PolicyEntry, quorum Gate 6, COLLECTING→STABILIZING evidence gate.
**Premise Rule & Dependencies** — Layer 4 dependency tracking, `depends_on` field on GroundedClaim, cycle-checked DAG, premise rule (HARD claims cannot depend on SOFT/STALE/INVALIDATED), BFS invalidation cascade (HARD→SOFT downgrade), CascadeEvent audit trail, Schema V5, quorum Gate 7.
**Agent Roles & Revalidation** — Layer 5 agent role assignment (PROPOSER/RETRIEVER/FALSIFIER/SYNTHESIZER), role budgets per risk level, quorum Gate 8, periodic revalidation orchestrator wiring TTL→AuditPipeline.
**ClaimStatus FSM Enforcement** — Layer 6 transition table (PROPOSED→SUPPORTED↔CONTESTED→{INVALIDATED|EXPIRED|REFUSED}), 9 TransitionReasons, guard validation, transition history, cascade SUPPORTED→STALE, terminal state HUMAN-only recovery.
**Autonomous Execution (A1-A4)** — Spine, InvariantSet, ExecutionBudget, SessionManager, AutonomousExecutor (step-function loop with spine+invariant enforcement, checkpoint/resume). Governor adapters wrap security/CFI/fiction/nonfiction/tone as Invariants.
**Invariant Store (Deferred 1)** — InvariantSpec (serializable), InvariantStore (file-per-item persistence), VALID_KINDS (6 factory mappings), materialization to live Invariant objects.
**Web UI (Deferred 2, W1-W3)** — GovernorContextManager (isolated per-user/project contexts), ChatBridge (Anthropic/Ollama abstraction), GovernorHooks (mode-specific system prompts), FastAPI adapter (OpenAI-compatible), Docker multi-user deployment.
**Structured Telemetry (Deferred 4, B2)** — TelemetryCollector + StructuredLogger (JSONL, date-partitioned), TelemetryEvent, cost/performance/convergence analysis, CSV/JSON export. Convergence telemetry: CONTINUITY_TRACE/RESULT events, ConvergenceExecutor, ConvergenceReport.
**Continuity Enforcement (Deferred 5)** — Closed-loop generation control. AnchorRegistry, ContinuityChecker, CorrectionLadder, ConvergenceExecutor, GenerationProvider Protocol. Continuity Bridges: mode-specific anchor factories, GovernorHooks integration.
**Evidence Gate** — Evidence-gated coding harness — kernel-only surface. HARD claims require evidence, contradictions persist, failures are loud. Custody scoring, claim extraction, evidence linking, contradiction detection, exit shape checking.
**W5 Writing Modules (Deferred 2, W5)** — 11 modules: writing_patterns (18 banks), writing_governance, writing_tone (ToneVector 6D, 16 regime envelopes), writing_regime (AffectRegime, RegimeHysteresis), writing_nonfiction (NfClaimLevel, PromotionGate), writing_intent (IntentClassifier, 12 scorers), writing_constraints (11 + Section 14), writing_ticketing (14 prose + 11 code), writing_puppet, writing_code, writing_router.
**Interactive Violation Resolution (W4)** — Author-friendly chat. Blocking violations present 3 choices: fix/revise/proceed. ViolationResolver with persistent state, resolution command detection, exception logging.
**Code Autopilot** — Intent-based governance. 5 profiles (greenfield/established/production/hotfix/refactor), intent resolution from 6 layers, constraint classes (invariant vs preference), scoped time-limited overrides with receipts, branch heuristics.
**External Constraint Attachment** — Bind claims to external substrate snapshots (Wikidata/Wikipedia/Scholar). NOT fact verification — structural constraint logging. Discrepancies surfaced, never auto-corrected. Human-only resolution.
**SDK Middleware** — Drop-in governor enforcement for Anthropic SDK. `client = GovernorMiddleware(Anthropic())`. Advisory/blocking/strict modes, claim extraction, anchor checking, security scanning, ledger integration, streaming + async.
**Session Continuity** — Capsule-based session management. Resume intent + constraints + authority, NOT chat replay. Three-layer model (Ledger/Workspace/Transcript), fork/promote semantics, checkpoints, content hashing, YAML ledger persistence.
**Git Governance** — Integrity invariants at commit boundaries. Artifact integrity, cross-index validation (DOI/version tags), tagging discipline, pre-commit provenance. Profile-based severity, YAML config, secrets check integration.
**Context Manifest (Phase 1)** — Prompt assembly as governed artifact. ContextRegion, ContextManifest (prompt_hash/manifest_hash/build_id), ManifestStore (JSONL+flock), build_manifest pure function. Wired into GovernorHooks._build_system_prompt(). Gate receipt verdict=observe.
**Context Compact** — Loss-aware context compaction with receipts. ContextCompactor, CompactionReceipt, DroppedItem, RecoveryStore, ReceiptStore. Preserves decisions/anchors/constraints/authority, emits explicit loss records.
**Perforce Support** — Integrity invariants on explicit authority substrate. P4Client (CLI wrapper, graceful fallback), P4Governor (changelist integrity, lock semantics, immutable releases, DOI mapping). Profile-based severity. P4 trigger integration.
**Gate Receipt System (receipt_v1)** — Content-addressed decision receipts. GateReceipt: receipt_id = H(schema_v + gate + subject_hash + evidence_hash + policy_hash) — timestamp is metadata. Canonical JSON. Split store: ReceiptStore (JSONL) + EvidenceStore (content-addressed blobs). All gates wired (evidence_gate, intent_compiler, pre_commit, wrapper, continuity_checker).
**Origin-Mode Operational Fence** — Closed-world admission predicate over the existing `origin_mode` discriminator (`cooked_context_orchestrator.py`; NQ_ORIGIN_MODES {observed,drill,replay,synthetic} + AG_INTERNAL {cli_origin,stub_origin}, custody-bound inside `evidence_hash`). `operational_admission()` admits **iff `origin_mode ∈ {observed}`** (allowlist, not blocklist); recognized-non-observed → `origin_not_operational`, novel string → `origin_unrecognized`, absent → `origin_missing`, non-str → `MalformedOriginError`. Offending mode carried verbatim in `OriginAdmission` (refused replay audits ≠ refused drill). Mandatory pinning test (`tests/test_origin_admission_fence.py`, 27 tests): novel string never admitted. Predicate shipped 2026-06-11. **Wired 2026-06-12 (Wall 1):** the orchestrator success edge applies the fence as a *type split* (option-B-amended) — `run()` returns `OperationalConsumed` (origin `observed`) vs `DemonstratedConsumed` (every other closed mode), distinct types, not a boolean; `confer_operational_effect()` is the spend wall that accepts only the operational type by `isinstance` (drills still mechanically consume + demonstrate structure, but cannot confer effect — unrepresentable, not merely guarded). Negative pinning tests in `tests/test_operational_spend_fence.py` (20). **Wall 2** (LA per-class unit matching → `unit_origin_mismatch`) is named, not built — cross-repo LA contract change (`working/candidate-la-unit-class-fence.md`). Rationale: `docs/constellation-zoning.md` §3 + §Evidence classes; wiring view in `docs/constellation-wire-plan.md`. (Note: `evidence_class` is the receipt-kernel blob-redaction field — NOT this; origin lives in `origin_mode`.)
**Standing-Spendability Gate (two-clock temporal lapse)** — `src/governor/standing_spendability.py`. The standing→spendability seam (post-admission, pre-spend) that refuses a spend whose standing observation lapsed past its horizon by exercise time (valid when observed, void when spent). `StandingWindow` (standing_observed / exercise: `MonotonicReading` + bound_ns + optional display-only `WallWitness` + optional capacity_commit). `evaluate_spendability_window()` (pure) + `StandingSpendabilityGate.check()` (emits a receipt on both paths carrying the two-clock "murder hallway" block: `gap_ns`, `bound_ns`, `overage_ns`, `lapse_coverage`, attested `gap_basis`, optional display-only `wall`). Refusal kind `standing_before_spendability_not_bounded` (AG-internal S4-lite addition, ratified 2026-06-12; distinct from `standing_expired` = unverifiable digest and `token_expired` = LA-side). NOT folded into `StandingClient.verify()` — witness exposes the clocks, policy (this gate) decides the gap (zoning §Standing). Composed by the orchestrator (optional `standing_spendability_gate` + `standing_window`); refuses before any capacity is spent. The demo's hero specimen, shipped as a PAIR (`temporal-lapse` impostor refuses / `temporal-lapse-twin` passes within horizon) — drill scenarios + golden corpus `08`/`09`. Tests: `tests/test_standing_spendability.py`, `tests/test_drill_temporal_lapse.py`, corpus block-pin (`tests/test_corpus_contract.py`) incl. the missing-`gap_basis` negative.
  **Clock-witness migration (2026-06-12)** — the gap basis moved from bare integer seconds (the "clock costume": a `clock_basis: str` tag riding alongside subtractable ints) to typed `MonotonicReading`/`WallWitness` in `src/governor/clock_witness.py`. *A gap is a difference between compatible clock witnesses, not numbers.* `elapsed_ns(start, end)` is the ONLY licensed subtraction and refuses incompatible bases — `GapBasisMismatch` (different source / backwards reading), `MonotonicEpochMismatch` (cross-reboot) — rather than producing a confident garbage number. The gap runs on monotonic readings sharing source + epoch (wall clocks step backward under NTP; a gap across a step is garbage with an ISO 8601 smile); wall time is a *different object* (display-only or freshness basis), never the gap basis. The mandatory-basis discipline is now enforced BY TYPE: a `StandingWindow` cannot be built from a number. Single-host demo basis is `process_monotonic` / `boot:demo-single-host` (multi-host later = value change, not schema change). Tests: `tests/test_clock_witness.py` (the incompatible-basis refusals — "two ugly tests"). Follow-on (spec'd, not built, `working/clock-witness-spec.md`): three-valued wall freshness over ±x bounds + timestamp-laundering refusals.
**Intent Compiler** — Structured hypothesis-collapse for governance sessions. IntentFormPolicy (TEMPLATE_ONLY/VALIDATED_CUSTOM/CUSTOM_OK), IntentFormSchema (content-addressed), 3 built-in templates, mode-gated form policy (blast radius proportional), deterministic compilation, 4-category escape classification.
**Governor Daemon** — JSON-RPC 2.0 control plane over stdio or Unix socket. Content-Length framing, async dispatcher, DaemonState (lazy-init subsystems), 36+ RPC methods (governor/sessions/intent/receipts/scars/correlator/scope/stability/commit/chat/etc.). ChatBridge integration with backend auto-detection, streaming via JSON-RPC notifications, GovernorHooks for governed generation, gate receipt emission. Guvnah via stdio child; Maude via Unix socket.
**Governed Shell (daemon surface, GS-2b..8, 2026-07-03)** — [Method count 91→99; GS-2b docket source + GS-7 autopilot RPC landed 2026-07-03 — see tail of this entry.] The operator-shell contract surface on the daemon (`docs/design/governed-shell/shell-contract-v0.md`; campaign `docs/campaigns/governed-shell/`). The unified operator decision feed + its one mutation door, so "the queue is true in the protocol, not just the UI". `operator_decisions.py` aggregates supervised interventions + promotions + the pending violation into shell-contract decision envelopes (mints nothing — every item is native-backed; `_gather_operator_feed` shared so list/watch/resolve can't drift). RPCs: `operator.decisions.list` (GS-2b), `operator.watch` (GS-4, bounded streaming; stable-projection digest excluding display-clock fields; notify-timeout so a stalled client can't outlive the loop), `operator.decisions.resolve` (GS-3, THE one mutation door — routes by trusted-feed item kind + option action to the backing handler, forwards the routed receipt, adds no refusal vocabulary, no privilege escalation via forged args), `runtime.session.send_input` + `OPERATOR_INPUT` event (GS-5, fail-closed operator input into a running backend — capability-gated on `supports_input_injection`), `runtime.adapters.list` + `why.chain` + `session.get` capabilities exposure (GS-6), `runtime.autopilot.get/set` (GS-7 — envelope-strip read + workspace-default profile switch reusing `set_intent`/`apply_autopilot_profile`; write-ahead profile-change receipt `gate="autopilot"` citing the operator via `resolve_principal`, `unknown_profile` closed refusal; workspace-scoped only — stop-condition-proven since the runtime supervisor never reads the autopilot/intent/envelope files; returns the EFFECTIVE resolution + explicit `requested_profile`=receipt-subject so a higher-priority intent layer can't silently diverge; FULL SANDWICH, three codex passes → MERGE-SAFE). Docket cases reach the feed via `DaemonState.docket_manager` (GS-2b docket source — resolver-unbound so a contested violation isn't double-surfaced; docket resolve route stays GS-3-remainder, door fails closed on it). Client library `libs/ag_shell_client/` (GS-8) de-triplicates the wire protocol (socket path proven byte-identical to `governor.daemon.default_socket_path`) + typed decision models with the safe-defaults idiom. Method count 91→99. Deferred: GS-2b remainder (admissibility source + HELD-launch state — re-tiered as authority-semantics, `OBSTRUCTION-gs2b-admissibility-held.md`), GS-8b (ag_shell_client live-socket client class), GS-9 (maude consumes the client — separate-repo UX).
**Correlator Telemetry** — Capture detection for governor-as-correlator. K-vector (Throughput, Fidelity, Authority, Cost) — never scalarized. Four capture indicators (Prop 4.2): mode_per_exposure decline, entropy non-increasing under exposure, generated contradiction suppression (Prop 5.3 inversion), commitment loss at boundaries. Hysteresis on all. SHEAR requires low throughput under blocking authority (cost budget → DEGRADED_CAPACITY flag, not SHEAR).
**Scope Governor** — Locality-first policy with escalation receipts. Constrains *where* agents act. Absence-restrictive containment (missing axis = locked, wildcard must be explicit `"*"`). Expanding rings ladder (resource→service→region→environment→tenant). Escalation = widen exactly one axis per request. Tool contracts bind scope. Time axis uses real timestamps (ISO 8601 UTC). Write/execute grants log every usage.
**Semantic Stability** — Perturbation-based conditioning audit. Mechanical perturbations (format jitter, clause reorder, role rewrap, hedge insert, negation probe). Four signals: stiffness (excess_divergence/magnitude), anisotropy (p90/p10), basin entropy, commutator drift. Noise floor baseline (intrinsic temperature variance subtracted). Negation probe as positive control (excluded from stiffness). Observational receipts (verdict always "observe"). Deterministic sampling (sha256, never Python hash).
**Receipt Kernel (libs/receipt_kernel)** — In-repo extracted library, stdlib-only. Append-only hash-chained event ledger (SQLite WAL), 7 event types (RUN_START, STAGE_ADVANCE, EVIDENCE_PUT, EVALUATION, DECISION, REMEDIATION, RUN_FINALIZE), content-addressed blob store with redaction hook + retention policy (TTL, hash-only after purge, BLOB_EXPIRE receipting). StageGraph hard-fails on illegal transitions. **13 constitutional invariants in 3 groups** — *structural* (6): ledger.chain_valid (hash chain integrity), receipt.completeness (evidence key + blob presence), evaluation.completeness (no silent downgrade), finalization.completeness (clean endings), run.single_finalize, run.stage_required_path; *hallucination* (6): claims_evidence_binding, tool_trace_consistency, epistemic_mode_requirements, refs_closed_world, output_bound_to_claims, confidence_sanity; *oracle* (1): oracle_independence. Verdict enum (PASS/WARN/FAIL/UNKNOWN — UNKNOWN is failure). Bridge adapter (`receipt_bridge.py`) for parallel emission. See `docs/RECEIPT_KERNEL_CONTRACT.md`.
**Lane Routing** — Capability-based lane routing with artifact reuse. Lane 0 (ROUTER), 1 (FAST), 2 (GENERAL), 3 (DEEP). LaneContract with must_have/nice_to_have/hard_disallow, ProbePolicy per lane. CascadeExecutor: generate→validate→mitigate-once→re-validate→escalate. ArtifactReuseStore: content-addressed, vary key includes model + prompts + tool schemas + doc hashes + policy/config versions. Autopilot levels 0/1/2.
**v2.4 Instrumentation Spine (Phase A)** — Windowed capture detection substrate. SignalEnvelope (23-field frozen dataclass, schema v0.4.0) with quality semantics (ok/partial/unavailable/invalid), canonical JSON hashing, JSONL emission. Three Phase A signals: EXPOSURE_PROXY (weighted denominator from tool dispatch, chat generation, evidence checks, violation evaluations), SILENT_SUPPRESSION (in-path health from multi-source indicators), SIGMA_RATE (endorsement→invalidation pair matching with lag statistics). Shared invariants: observe-only (no blocking), missing≠zero, versioned semantics, receipt-linkable provenance, window boundary semantics (inclusive start, exclusive end). All signals pure derivation (no IO).
**Phase B1 CAPTURE_SELF_DIAGNOSTIC** — Advisory windowed diagnostic consuming Phase A signal envelopes only (layering invariant). Six classifications: normal, watch, warning, instrumentation_compromised, insufficient_history, indeterminate. Suppression precedence (A2 gates all capture scoring). Weighted scoring: sigma rate (0.7) + coverage gap (0.3) → capture_decline_score [0.0, 1.0]. Threshold config versioned as capture-selfdiag-v1 (pre-calibration heuristics, pending Phase C). Completeness = min of input completeness. Input content hashes in annotations for replay provenance. B2 and B3 shipped.
**Phase B2 DECISION_EVIDENCE_LAG** — Per-decision timing classification from gate receipt pairs (receipt-native, parallel to B1, not layered on A signals). Four classifications: SUPPORTED_AS_OF (evidence before decision), BACKFILLED (evidence after), UNSUPPORTED (no linked evidence), POLICY_EXEMPT (gate in exempt list). Tie-break: same-timestamp → SUPPORTED_AS_OF. Multiple evidence → earliest wins. Policy-exempt excluded from rate denominator. value = backfill_rate [0.0, 1.0] or None. Config versioned as decision-evidence-lag-v1 (pre-calibration). Windowed aggregate with lag statistics (mean/p95 backfill delay and support staleness). B3 shipped.
**Phase B3 POSTERIOR_SHIFT_ATTRIBUTION** — Leave-one-out (LOO) influence at the A-signal level. Answers "how much does each Phase A signal influence B1?" by removing each signal and recomputing the entire B1 derivation — no shortcuts. Influence dataclass (signal_id, score_full, score_minus, delta_raw, direction, rank, optional calibration fields). compute_loo_influences() performs n+1 B1 derivations. Non-conservation by design (influences, not partitions). Ranking by |delta_raw| descending, ties broken alphabetically. Optional calibration pass-through (monotone preserves sign). Suppression precedence: removing A2 makes B1 unscoreable, so A2 is always highest influence when present. value = influence_mass (sum of |delta_raw|), unit = "influence".
**Phase C2 CALIBRATION_LAYER** — Apply-only calibration for raw A/B signals. Normalizes signal values to [0,1] using frozen, versioned parameter sets. Three transform methods: identity_clip (pass-through with clamping), linear_minmax (linear rescaling from observed range), log_minmax (log-space rescaling with explicit epsilon_shift — user-pinned policy). CalibrationParamSet (frozen dataclass, MappingProxyType copy+wrap for true immutability, content-addressed param_set_hash). CalibrationMismatchError (exception, not degraded output). Bounds validation: clip bounds enforced in [0,1], inverted bounds refused, bool excluded from numeric check. Quality propagation: ok/partial calibrated normally, unavailable/invalid → value=None. Companion envelope builder (phase="2.4C", unit="normalized"). Missing≠zero preserved.
**Phase D PREDICT_REGIME_PREFLIGHT** — Observe-only pre-session regime prediction from calibrated A/B signal envelopes. Pure function, no IO, no hidden state. 6-regime taxonomy (normal, watch, warning, instrumentation_compromised, insufficient_history, indeterminate). Weighted heuristic: capture_diagnostic (0.40) + sigma_rate (0.30) + decision_lag (0.20) + exposure_proxy (0.10). Threshold-based classification with configurable boundaries. Suppression precedence (SILENT_SUPPRESSION + CAPTURE_SELF_DIAGNOSTIC gate all scoring). Confidence from input completeness + quality + hard caps. Missing≠zero (unavailable → value=None). Provenance closure (input envelope hashes, config version, quality statuses). Goldens for all 4 primary regimes.
**Verified Kernel 2.x Hygiene** — Canonicalization monoculture sweep. HashRef value type (`hash_ref.py`) for cross-module hash comparison — parses both `sha256:<hex>` (signals, receipt_kernel) and raw hex (gate_receipt), compares by `(alg, digest)`. Non-canonical `content_hash()` renamed to `dedup_fingerprint()` in 4 modules (session_continuity, scalar_collapse, webui_demo, commitment_transport) to prevent accidental promotion into content-addressing. Canonical implementations (gate_receipt, signals/envelope, receipt_kernel) left unchanged — stored hashes not rewritten.
**Verifier Gate** — Composition boundary for mechanical verification. VerifierSuite (frozen, content-addressed policy_version), Candidate (subject under test), CheckOutcome (tri-state with detail), RunStatus/SuiteVerdict enums. run_suite() executes checks with timeout enforcement, flake detection (quarantine after N consecutive failures), environment fingerprint gating. Gate receipt emission (fail-open). Monotonicity property: superset of checks cannot weaken verdict. VERIFY_SUMMARY signal emission (one envelope per suite run, value = count_block + count_error, timing fragment, source_receipt_ids).
**Governed Activities** — Drift-gated retry substrate. FactObservation (ops-native observation), PreconditionBundle (sorted, fingerprinted), AttemptRecord (per-attempt with etag snapshot), DriftCheckResult (tiered verdict). Tiered drift detection: etag overlap first → fingerprint fallback. DriftVerdict enum (no_drift, etag_diverged, fingerprint_diverged, no_prior, continuity_lost). JSONL store with receipt emission tracking. Etag key = fact_type:subject:source (excludes request_fingerprint by design).
**Signal Plane v1** — SQLite projection cache over instrumentation JSONL. SignalStore with byte-offset cursor, inode/shrink detection, transactional ingest (BEGIN IMMEDIATE), fcntl.flock write exclusivity. project_envelope() pure function. Query/get/tail/stats/rebuild. CLI: `governor signals list|tail|explain|stats|rebuild`, renamed `signals` → `claim-signals` for claim extraction. Daemon RPC: signals.query/get/tail/stats. Tail with --name filter and --poll-ms follow mode. SIGNAL_EMIT_FAILED self-diagnostic (emission failures queryable in same plane, no recursion).
**Process Session Identity** — Canonical process-scoped session_id for signal/receipt correlation. get_session_id() (lazy, stable per process), set_session_id() (override), new_session_id() (fresh without setting global). "gov_{uuid12}" format. DaemonState owns one for its lifetime, exposed in governor.hello RPC. JsonlSink threads session_id into SIGNAL_EMIT_FAILED.
**GATE_CHECK_SUMMARY (first live signal)** — One SignalEnvelope per `gate.check()` invocation. build_gate_check_summary (pure) + build_gate_check_error_summary (for exceptions) + try_emit_gate_check_summary (fail-open wrapper). Wired into CLI `lite_check` with timing. Values: verdict, claims_count, violations_count, warnings_count, has_oracle, timing_ms. Quality: ok for successful checks, unavailable for exceptions. Provenance: source_versions via default_source_versions(), session_id from get_session_id().
**Signal Provenance Tightening** — source_versions populated with default_source_versions() (governor pkg version + envelope_schema version) across all signal builders. source_receipt_ids monotonic propagation: Phase A accepts, B1 unions from A inputs, C calibration preserves, D unions from all inputs. session_id in Phase D: explicit parameter or inferred from most recent input envelope.
**Provenance Labels** — Lightweight taint tracking for tool outputs. ProvenanceLabel (frozen dataclass, validated source_class + sensitivity_hint). 7 source classes (repo, email, web, secret_store, user_input, generated, unknown), 4 sensitivity hints (none, internal, secret_candidate, unknown). max_sensitivity() conservative propagation (output inherits highest of all inputs). LabelAssigner: mechanical rule-based assignment from tool_id + context, 19 known tool mappings, 15 secret path patterns, 8 internal URL patterns, extensible via extra_tool_map and extra_secret_patterns. Wired into evidence gate as thin annotation layer: labels assigned per check() invocation, included in evidence bundle, never affect gate verdict.
**Egress Gate** — Outbound data-flow policy gate. Policy bridge: classify → populate existing policy types → evaluate → emit gate receipt. PayloadClassifier (monotone: label sensitivity > path patterns > regex > default, never downgrades). DestinationClassifier (URL/IP/hostname parsing, internal TLD detection, RFC 1918, normalization). 6 rules in explicit precedence order: R1 secret deny, R3 unknown dest, R5 allowlisted, R4 internal allow, R2 sensitive external, R6 default. Strict mode = WARN→BLOCK upgrade only. Allowlist = exact or suffix match (leading dot), UNCLASSIFIED only. No USER_REQUESTED bypass (justification recorded, verdict unchanged). Receipt redaction: hashes + classes only, no raw destinations/content/paths. Content probe opt-in (regex secret scan, can only upgrade). Stateless, observe-only.
**CI Lane** — Receipt-producing CI wrapper and policy verifier. `ci_wrap()` runs commands via temp-file capture (no pipe deadlock), emits self-contained `CiReceiptBundle` (evidence inlined). `ci_verify()` checks policy: required kinds, all-pass, SHA consistency, clean state, conflicting-ID detection (identical-bundle dedupe OK). Closed ci_kind vocabulary (8 kinds). Fail-open wrap (exit code always child's), fail-closed verify. CLI: `governor wrap --receipt-out --ci-kind`, `governor ci verify`. (`ci_wrap` extended 2026-06-12 with optional `extra_evidence`/`gate`/`subject_kind` for the verifier wrapper — defaults preserve CI-lane behavior.)
**Verifier Wrapper** — Mechanical guard against masked exit codes ("a verifier result is admissible only when the verifier's own exit status is observed"). `src/governor/verify.py`: `analyze_command()` classifies a command's exit-source (direct exec → `child_exit`; shell `-c` pipe without pipefail → `masked_exit_risk`/`unknown`; pipe with `pipefail`/`PIPESTATUS` → `pipefail_preserved`); `verify_run()` REFUSES a masked-risk pipeline before running it (emits a `block` receipt, `verifier_exit_observed=false`) unless `--allow-masked`, else runs via `ci_wrap` (child exit captured directly) and emits a `verifier` receipt carrying `verifier_exit_observed` / `verifier_exit_source` / `masked_exit_risk` + cwd/argv/duration/log-digest. CLI: `governor verify-run -- <cmd>`. Loop doctrine (`docs/loop-protocol.md` §3): AUDIT refuses a green with no verifier receipt or `masked_exit_risk: true`. Scar-derived (migration 058 shipped with three red tests masked by `cargo test | tail`, 2026-06-12); global rule in `~/.claude/CLAUDE.md` § Verification discipline. Tests: `tests/test_verify.py` (failing-piped-to-tail refused, failing-direct is fail, passing-direct is pass, pipefail-preserved failing recorded as fail).
**Sim→Signal SIGMA_RATE** — Second Phase A signal derived from sim receipts alongside EXPOSURE_PROXY. Receipts mapped to ReceiptEvent, match_sigma_pairs() finds endorsement→invalidation pairs, derive_sigma_rate() uses EXPOSURE_PROXY value as preferred denominator. Contradiction scenarios produce nonzero sigma rate. Deterministic (pinned emitted_at).
**Operational SLA Gap Spec** — Two-path availability contracts for governor as control plane. Decision path (fast, p99 budget in ms, fail-closed or fail-open+debt receipt) vs evidence path (slower, accountable, debt tracking). Per-lane SLO routing. Temporal coherence constraints (policy freshness, index lag). Timing fragment on gate receipts (make_timing with monotonic_ns). Gap spec at specs/gaps/OPERATIONAL_SLA.md.
**Runtime Supervisor** — Governed sessions over external agent CLIs. Canonical event bus (JSONL persistence, monotonic seq, tool_call_id dedup, cursor reads). RuntimeAdapter protocol with declared capabilities. Claude Code adapter (supervised mode, Unix socket hooks, PascalCase hook format). Gemini CLI adapter (BeforeTool/AfterTool hooks, exit code 2 blocking, GEMINI_CLI_SYSTEM_SETTINGS_PATH isolation). SessionSupervisor with lifecycle FSM (created→launching→attaching→running→exited/failed), intervention queue with configurable timeouts, auto-approve for read-only tools. Promotions: workspace diff detection via git, approve/reject/revert, junk file filtering. Session forking from promoted parents with explicit lineage. Settings cleanup (atexit handler + CLI command). 13 daemon RPC methods (runtime.session.*, runtime.intervention.*, runtime.promotion.*). Dogfood-verified: supervised Claude Code and Gemini CLI sessions including denial, timeout, intervention, and full promotion workflow. Maude integration: 12 `supervised` commands + `snapshot`/`wtf` operator overview. Fail-closed pre-tool gate (Tock 1, 2026-06-10): hook denies with explicit reason on decision timeout / socket error / missing config / garbled response; decision window threaded supervisor→adapter via `GOVERNOR_DECISION_TIMEOUT`; Claude Code settings hook-timeout sits behind the script's deny deadline (its kill-and-proceed was a second fail-open). Drill-verified with absent operator (`working/tock-01-fail-closed-gate.md`). Gemini adapter same fail-open class still open (GAP-M). Session-attributable promotion (Tock 2, 2026-06-10, GAP-N): `snapshot_dirty_paths` baseline at launch; dirty tree refused by default (`DirtyWorktreeError`) or fenced via `allow_dirty`; `detect_workspace_changes(baseline=)` excludes pre-existing dirty (`Promotion.excluded_files`); `revert_paths` reverts only session paths (reject can't destroy prior uncommitted work). `runtime.session.create` + `runtime launch --allow-dirty`. `working/tock-02-session-attributable-promotion.md`.
**Standing-Class Chain Validator (Constitutional C2)** — `src/governor/standing/` package implementing the validator contract from `docs/doctrine/validator_contract.md` against the four ratified Q1–Q4 decisions (kernel composition, subject_derivation enum, exception-class registry, validator provenance). Closed enums (StandingClass×6, ReceiptRole×7, SubjectDerivationKind×4, ValidationOutcome×5, AuthorizationVerdict×4); closed `ViolationCode` vocabulary with `ViolationClass` mapping (no stringly-typed buckets); StandingChainValidator with bootstrap-fail-closed startup (refuses to run without bootstrap policy_declaration or on `expected_ruleset_hash` mismatch); checks: role↔standing mapping, parentage transitions (§5.1, §5.4), chain integrity (§6 content-bound parents + cycle detection), subject_derivation mechanical checks per Q2 (same_subject byte-equality, scope_narrowing prefix containment, aggregation_of completeness), policy binding (§8 unknown artifact / hash mismatch / ontology version), compressed-path admissibility (Q3 empty initial registry — every compressed authorization rejected until at least one exception class declared); validation receipts carry every Q4 mandatory field (validator_id, validator_version, ruleset_hash, policy_registry_hash, validated_at, target_receipts, outcome, violations, exceptions). `kernel_bridge` is pure translation — payload-overload onto receipt_kernel events per Q1.A working choice, no policy lookup. Bootstrap policy_declaration `decision.validator.v0_1_0` (`docs/doctrine/decisions/validator-v0_1_0.md`) closes Q4 chicken-and-egg with one bounded sanctioned hole. No Q5 pre-ratification fallbacks anywhere — ratified rules are final.
**Standing Schema Discipline (Constitutional C3)** — `src/governor/standing/schema.py` + envelope hardening on `types.py`. Strict `sha256:[64-hex]` content-hash format on every parent_ref / policy_artifact_hash / self-content_hash (`HASH_FORMAT_INVALID` chain class). Structured `Check`/`CheckResultStatus` dataclasses; AUTHORIZE receipts must carry the four `AUTHORIZE_REQUIRED_CHECKS` (standing/admissibility/scope/budget) per validator_contract §9 — boolean-only or ID-only check fields are insufficient. New ViolationCodes: `HASH_FORMAT_INVALID`, `AUTHORIZATION_CHECK_MISSING`, `AUTHORIZATION_CHECK_MALFORMED`, `UNKNOWN_FIELD_IN_ENVELOPE`, `MISSING_REQUIRED_COMMON_FIELD`, `INVALID_FIELD_TYPE`. Closed envelope (`STANDING_RECEIPT_ENVELOPE_KEYS`) — `StandingReceipt.from_dict` rejects unknown keys, missing required common fields, wrong types with typed `Violation` instances bundled in `EnvelopeParseError` (hostile-input discipline; no best-effort normalization). Roundtrip property: `from_dict(to_dict(r))` produces a value whose canonical body byte-equals the original's. Schema pre-pass in `StandingChainValidator.validate()` — schema violations surface alongside parentage / chain / policy checks. **Validator bumped to v0.2.0** with `decision.validator.v0_2_0` (`docs/doctrine/decisions/validator-v0_2_0.md`) carrying `supersedes: decision.validator.v0_1_0` — first exercise of Q4 supersession discipline. Bootstrap exemption is non-transitive: v0.2.0 admissibility depends on a v0.1.0-produced validation receipt at `_validations/decision.validator.v0_2_0.json`; missing/malformed/forged receipt → BootstrapError at startup with no interpretive dance. Tamper-evidence comes from receipt's `target_receipts.content_hash` binding to the declaration's bytes (no separate receipt_hash, avoids hash-cycle).
**Standing Check Basis Discipline (Constitutional C4)** — `Check.basis` is no longer a freeform `str`; it is a structured `CheckBasis` (summary + rule_id + non-empty `inspectable_refs`). Falsification: `{"result":"pass","basis":"seemed fine"}` parsed cleanly under C3 and is rejected with `AUTHORIZATION_CHECK_MALFORMED` under C4. Reuses the existing ViolationCode — no new vocabulary added (ChatGPT-discipline). `CheckBasis.from_dict` validates structural shape (closed key set, all three fields present and non-empty, refs are non-empty list of non-empty strings). Schema pre-pass `_check_authorize_checks` defensively catches in-code construction with non-`CheckBasis` basis. **Validator bumped to v0.3.0** with `decision.validator.v0_3_0` (`docs/doctrine/decisions/validator-v0_3_0.md`) carrying `supersedes: decision.validator.v0_2_0` — second iteration of the supersession ceremony, confirms it is a repeatable pattern not a one-shot move. v0.2.0-attestation receipt minted via the canonical regen script at `_validations/decision.validator.v0_3_0.json`. SupersessionCeremony tests refactored to be version-agnostic (`_active_bootstrap_filename()`); future v0.4.0+ pick them up automatically. Test-corpus naming convention extended to `<violation_code>__<note>.json` for multiple fixtures per code; new fixture `authorization_check_malformed__basis_string_seemed_fine.json` is the frozen falsification target. Subject_derivation `basis` field per Q2 ratification is **untouched** — that field is descriptive prose, not a validator input. C4's structuring applies only to `Check.basis` on AUTHORIZE checks.
**Standing Continuity Basis (Constitutional C5)** — first commit of the **continuity-facing phase** (validator-core phase ended at C4). `StandingReceipt.continuity_basis: ContinuityBasis | None` is a presence-as-claim block per validator_contract §10. New types: `ContinuityBasis` (all-or-nothing container), `BasisRecord` (same shape as `CheckBasis`, distinct type — ChatGPT rule: shared helper `_validate_basis_block`, no shared dataclass). Four required sub-bases: `identity_basis`, `provenance_basis`, `evidence_basis`, `operator_confidence_basis`. **Role gate**: `CONTINUITY_CLAIMABLE_ROLES = {recommendation, authorization, action}`; `observation`/`interpretation`/`policy_declaration`/`validation` carrying `continuity_basis` → `CONTINUITY_BASIS_ROLE_NOT_ELIGIBLE`. **Two new ViolationCodes** (forced by role gate, no existing code captured "field present on wrong role"): `CONTINUITY_BASIS_ROLE_NOT_ELIGIBLE`, `CONTINUITY_BASIS_MALFORMED` (bundles null/empty/partial/missing-sub-basis/string-sub-basis). `operator_confidence_basis` is **descriptive only** — validator enforces presence + structure, does not read value as a weighting signal (promotion to adjudicative would be a hotter seam requiring its own Q4 supersession). Hostile-input discipline extended: `continuity_basis: null` and `continuity_basis: {}` rejected (presence-as-claim cannot be loophole'd). Roundtrip: canonical_body drops None continuity_basis so absence-as-no-claim survives serialize/deserialize. **Validator bumped to v0.4.0** with `decision.validator.v0_4_0` (`docs/doctrine/decisions/validator-v0_4_0.md`) carrying `supersedes: decision.validator.v0_3_0` — third iteration of the supersession ceremony, pattern is now routine infrastructure. v0.3.0-attestation receipt at `_validations/decision.validator.v0_4_0.json`. 5 new corpus fixtures (1 good action_with_continuity_basis + 4 bad continuity_basis variants exercising the parse-time and runtime rejection paths). Corpus framework extended: bad fixtures may fail at parse OR runtime; both are typed rejections. Subject_derivation `basis` (Q2 descriptive prose) and `Check.basis` (C4 verdict structure) are **distinct surfaces** — ChatGPT's "different species" rule held.

**Workflow-Kernel / Self-Annealing Spine (2026-06-13)** — the rung-activation + recomposition-enforcement family from `working/campaign-workflow-kernel-annealing.md` (specs/core/SELF_GOVERNANCE_SPEC implementation track). *Pipeline types* (`pipeline_types.py`): `RecompositionReceipt` + pure/total `account_boundaries(admitted, accounted)` — the laundering refusal (`refused_laundering` when an admitted decomposition boundary is unaccounted). *Recomposition enforcement* (P3.2, `cooked_context_orchestrator.py`): opt-in `run(recomposition_plan=...)` flips shadow→effective; a laundered would-be-success returns a `RecompositionRefusal` (seam `recomposition_seam`) that the spend type-wall refuses — recomposition's only verb is refuse. *DebtLedger* (P3.0b, `debt_ledger.py`): the authoritative live claim source (flock-guarded `record`/`discharge`/`open_claims`); future-rung debt mints a `NonDischargeClaim` here (binds a collector ≠ target rung, blocks the rung), never commit-body prose. *Activation preflight* (P3.0, `activation_preflight.py`): `RungDebt`, `check_activation_eligibility`, `TARGET_ALLOWLISTS`. *Four-office activation* (P3.1, `activation.py`): a scoped transaction for exactly ONE tunable (`decomposition_size/max_slices`) across admissibility · act-standing · exactly-once spend · durable custody — BOOTSTRAP grade (operator-fiat standing stub, local spend ledger, local receipt store; external Standing/LA/NQ refused via `REFUSED_DEGRADED_CLAIMS_BACKING`). Reads live claims at the gate; custody-anchored + effect-surface-fenced writes; receipt-derived rollback. *Annealing delta* (P2.x, `annealing.py`): `AnnealingDelta` custody (HardGuards forced-true, tunable allowlist, no write path; must not import `convergence_tuning`). *Decomposition completeness* (P3.3, `decomposition_completeness.py`): the receipt-shape valve — every path to `complete` (enumeration/coverage) requires a structured evidence object; AG-alone emits `enumeration=declared`/`coverage=best_effort`, no bare `decomposition_complete` scalar. Cross-tool doctrine in `docs/cross-tool/` (rung-activation four-office, receipt-sovereignty/microkernel, decomposition-capability-closure, symbolic-instrument-witness, hotpath-and-granularity). **Bootstrap limits (documented, accepted):** in-process custody/evidence forgeability (P3.1/P3.3 fence the SHAPE, not provenance); operator-fiat standing; constellation offices unwired; receipt-kernel ratification-invariant not yet superseded; Rust extraction not started. P4 (promotion/expiry) NOT started.

### current research

Offline analysis / exploration / productized-but-not-live tuning. Promote to `candidate substrate` or `3.x platform` only when wired into live platform behavior (auto-apply, runtime feedback loop, automatic receipt emission).

**Auto-Tuning** — Threshold learning from signal distributions, reset effectiveness tracking, setpoint calibration from baselines, budget sweep with Pareto analysis.
**Convergence Auto-Tuning (Deferred 6)** — Offline system identification over convergence traces. ConvergenceAnalyzer, TuningProposal (28 dataclass types, HardGuards forced-True, Approval forced-requires_human), ProposalStore, 5 admissibility checks, 10 footgun guardrails.
**Phase C1 REPLAY_HARNESS** — Deterministic offline replay of signal derivations under alternative thresholds or matching rules. Two explicit modes: envelope replay (for layered signals like B1) and receipt replay (for parallel signals like B2). ReplaySpec (frozen config), ReplayManifest (content-addressed input inventory, order-independent hash), DerivationEntry registry keyed on (signal_id, signal_version). Deterministic window ordering. Five skip reasons: missing_inputs, suppressed_excluded, quality_filtered, alignment_failure, derivation_error. Per-window outputs preserve original signal_id and carry replay provenance (phase="2.4C", replay_run_id, source_signal_hash). Summary REPLAY_HARNESS envelope with drift statistics. Key invariants: observe-only, no mutation of source artifacts, deterministic, provenance closure, missing≠zero.
**Phase C2 CALIBRATION_FITTING** — Offline param-set fitting from replay corpus. CalibrationFitSpec (frozen config with content-addressed hash), deterministic sample extraction with explicit exclusion reason taxonomy (8 reasons, one per dropped item, no silent drops). Three method fits: identity_clip (fixed clip bounds), linear_minmax (corpus min/max), log_minmax (domain-filtered with explicit epsilon_shift). FitResult structured return (param_set on success, summary_envelope always). Fit summary as SignalEnvelope (CALIBRATION_FIT_SUMMARY, phase="2.4C") with corpus stats, exclusion counts, provenance hashes, degenerate_fit flag. Suppression-aware by default. No param set emitted on failure. Integration gate: fitted param sets accepted by apply_calibration().

### candidate substrate

Items that exist as substrate for later promotion or absorption but are not currently on the live governance hot path. Analysis surfaces feeding governance, voice/output constraints, observability tools, and test infrastructure live here.

**Direction Tracking** — Commitments, anchors, Δt measurement, belief graph triangulation.
**Claim Diff** — Epistemic state change detection, confidence drift, provenance laundering, evidence erosion, silent retraction.
**Claim Signals** — Implicit claim extraction from text, date/entity/quantity/assertive detection, assertiveness scoring, ledger integration.
**Puppet Mode** — Persona pinning, voice constraints, epistemic posture, semantic diff guard (7 rules + 2 warnings), answer skeleton, 3 builtin profiles, registry.
**Tainted Claim Similarity** — Token-set Jaccard fingerprinting, inverted index candidate retrieval, exact/near-duplicate detection, audit events, configurable thresholds.
**Tone Profiling** — ToneProfile (28 dimensions), ToneChecker, tone guidance generation, ToneManager persistence, corpus analysis, profile comparison.
**Strategic Test Suites** — Golden-file (JSON schema locking), no-laundering regression, failure-injection, property-based invariant fuzzing, contract tests for adapters.
**Telemetry Dashboard** — Rich TUI for real-time regime visualization. Phase space plot, regime gauge, energy sparkline, event log, budget panel. Live/replay/demo modes.
**Prometheus Metrics** — Optional Prometheus metrics export at /metrics. Counters, histograms, gauges. TelemetryCollector backend integration.
**Interferometry** — Multi-model claim comparison with parallel + serial ("yes, and") modes. Claim extraction via SignalExtractor, Jaccard fingerprinting via taint module, claim alignment (shared/unique/conflicting), ledger promotion. JSON persistence.
**Code Interferometry** — Code-specific divergence analysis (Tier 1/2). 19 risk markers (security/edge-case/architectural), anchor compatibility (hard/soft conflicts), tier determination, CheckFinding bridge for VS Code. CLI `governor interferometry compare` + `governor code compare` alias.
**QA Harness** — Self-validating test infrastructure. CLI smoke tests, self-governance (governor passes own gates), serialization roundtrip sweep, cross-module lifecycle tests.
**Maude Contract Tests** — Provider-side contract tests verifying Maude's Pydantic models deserialize Governor daemon's actual RPC responses. Docker-compose (daemon + test runner, shared Unix socket volume). Backend smoke stubs skipped by default.
**Test Hardening (v2.0.2)** — Fresh-clone smoke tests (CLI happy path + daemon stdio via subprocess, @smoke marker). Adversarial hook bypass tests (symlinks, script tampering, malformed payloads, unicode tricks, --no-verify documentation). Upgrade path tests (SQLite V1/V3/V5→V6 migration, receipt forward compat, session capsule compat, from_dict robustness). Scale/performance tests (10k receipts, 1k claims, SQLite concurrency with 20 threads, 1MB security scan, 100-anchor continuity check, @scale marker). CI matrix expanded to macOS + Python 3.13.

## Retired

Retirement is an *orientation signal*, not a deletion instruction (per §7.2 of `GOV_GAP_FEATURE_HISTORY_LIFECYCLE_001`). Retired entries may still describe code that runs, doctrine that mattered historically, or features that can be revived. Reason prefixes from the closed-ish vocabulary in §7.1.

### VS Code Extension (Deferred 3, V1-V4) [RETIRED 2026-05-26: ABSORBED_INTO vscode-governor — separate repo; `governor check` CLI remains in core]

Extracted to [github.com/unpingable/vscode-governor](https://github.com/unpingable/vscode-governor). CLI `governor check` remains in core. Extension uses CLI wrapper (CLI-based, not daemon-dependent).

### WebUI Backend Toggle [RETIRED 2026-05-26: ABSORBED_INTO gov-webui — chat_bridge backend capability remains in core]

Runtime backend switching via sidebar dropdown. `GET /v1/backends`, `POST /v1/backends/switch`.

### MCP Safety Controls [RETIRED 2026-06-16: REMOVED_UNUSED — code deleted, see specs/gaps/GOV_GAP_MCP_SAFETY_DISPOSITION_001.md]

RateLimiter, BackpressureController, CircuitBreaker, IdempotencyLayer, LatencyEnforcer, FaultHandler (sensor/actuator classification), SafetyController (unified). Was listed as 3.x platform but had **zero production consumers** — never imported by `mcp_server.py`, the daemon, or any gate; not in `__init__.py`; no entry point; no config reference. `src/governor/mcp_safety.py` + `tests/test_mcp_safety.py` deleted 2026-06-16 after the operational-authority census (`working/campaign-operational-authority-census.md`) found it shipped-dead-and-undispositioned. **The capability class is retired, not the idea** — resurrection condition in the gap spec. Implementation was generic (token bucket / circuit-breaker FSM / idempotency cache / latency deadline); rebuild from the operational seam when a measured daemon/MCP failure mode forces it.

## Follow-up audits (not gap closure work)

The execution pass surfaced two questions out of scope for the lifecycle gap:

1. **Boil Control vs. Regime/Ultrastability/Homeostat/Coupling supersession.** Classified by criteria as `3.x platform` (criteria stop at bin assignment per §7.2). Whether Boil Control is functionally superseded by the newer governance family is a separate audit.
2. **Test infrastructure home.** Strategic Test Suites / QA Harness / Maude Contract Tests / Test Hardening all classified as `candidate substrate` because the bins have no native "permanent supporting infrastructure" category. If this turns out to be a recurrent mismatch, the bins may want a fifth category, or the `candidate substrate` definition may need to explicitly include durable test/CI infrastructure.

Several entries (Direction Tracking, Claim Diff, Claim Signals, Tainted Claim Similarity) classified as `candidate substrate` on the conservative read that their CLI surfaces are user-invoked rather than auto-fired at runtime. If grep confirms automatic firing, promote individually to `3.x platform`.
