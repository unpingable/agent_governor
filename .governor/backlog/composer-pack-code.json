{
 "id": "composer-pack-code",
 "kind": "candidate",
 "spec_ref": "working/candidate-constellation-composer-2026-07-15.md",
 "priority_tier": 2,
 "status": "filed",
 "effort_band": "M",
 "canonical_source": "working/candidate-constellation-composer-2026-07-15.md",
 "open_items": "CANDIDATE domain pack, named 2026-07-15 (operator: 'coding is also important, but that deserves its own slice'; then 'there's plenty to justify code, given we use it for code already' — the forcing case is real: AG governs code daily). Local ontology per chatty: source/artifact identity · build graph · specification and invariants · compiler/test/verifier witnesses · dependency and version constraints · change impact · release authority. AUDIT RUN 2026-07-15 (as the stub prescribed) — completion-redshift CONFIRMED plus one real gap: the witness layer is largely built (spine.py, invariants.py, verifier_gate.py, oracle_pytest.py, evidence_gate.py, ci.py, verify.py), but `oracle_class` — the independence ladder itself — is SELF-ATTESTED end to end (OraclePytestRunner.__init__ takes it as a parameter → OraclePytestLog → evidence blob meta → receipt_kernel build_blob_class_map → oracle_independence invariant → release_taint publish threshold). Belief.source='witnessed' in a hard hat. LATENT (thresholds ship at class 0, inert) but live the moment anyone raises the bar as the docstring invites. Finding + 4 options: working/finding-oracle-class-self-attested-2026-07-15.md — REQUIRES A RULING (touches receipt-kernel constitutional invariants + the publish boundary = custody-affecting). The pack stays UNBUILT; this audit found the gap it predicted, it did not authorize the pack.",
 "repo": "agent_gov",
 "wake_condition": "a ruling on working/finding-oracle-class-self-attested-2026-07-15.md (the independence ladder is self-attested — custody-affecting: kernel invariants + publish boundary), THEN operator selection. Per the 2026-07-15 sequencing rule, the pack still earns its own specimen, ontology, witnesses, and compiled projection; abstraction only after the third instance. Fiction (knowledge.py) is instance 1.",
 "reconciled": {
  "date": "2026-07-15",
  "basis": "audit executed 2026-07-15 per the stub's own precondition; gap traced to file:line across oracle_pytest.py / evidence_gate.py / receipt_kernel _helpers.py / oracle_independence.py / release_taint.py; nothing built",
  "confidence": "CURRENT"
 }
}
