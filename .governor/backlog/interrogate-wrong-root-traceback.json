{
  "id": "interrogate-wrong-root-traceback",
  "repo": "agent_gov",
  "kind": "build_slice",
  "priority_tier": 4,
  "status": "queued",
  "spec_ref": "found during readme-venv-and-interrogate-wording live verification 2026-06-12",
  "acceptance": "demo/interrogate.sh (governor.demo_interrogate) given a wrong/nonexistent root exits nonzero with a one-line human message naming the expected layout (the root Act 1 printed, not its /impostor subdir), no raw traceback.",
  "note": "Reproduction: ./demo/interrogate.sh /tmp/<act1-root>/impostor -> FileNotFoundError traceback (it appends /impostor itself). Plausible stranger confusion: Act 1's other printed commands use --root <root>/impostor, the interrogate line uses <root>. Hostile/wrong-input polish; the happy paths (no-arg, printed root) are verified live and hold."
}
