# Status — docs-professionalization

Card ratified 2026-07-05. Layout + language table ratified by operator
(relayed in-session); integrator refinements: link-preservation hard rule,
operational dirs don't move, top-layer-register not global rename.

## Phase D-pre — DONE (2026-07-05)

- **D-1 DONE** (unpingable-site `810fc62`): the two-line product statement
  leads the front page ("A way to let AI tools help with operational work…
  Agents can propose work. They cannot approve themselves."); the custody
  hero becomes the second beat.
- **D-2 DONE** (AG `8174af2`): `docs/start-here/` quartet —
  what-this-is.md, 10-minute-inspect.md, 30-minute-run.md, concepts.md.
  Plain operator register (language table applied; every claim cashes in a
  linked doc; sharp-parts listed verbatim-in-force in concepts.md). Link
  targets verified present.
- **D-3 DONE** (unpingable-site `bb0a9a8`): plain→precise bridge table at
  the top of glossary.html. (No repo-side glossary exists in agent_gov —
  noted, not created.)
- **D-4 DONE** (AG `8174af2`): README front gains a one-line "Start here"
  block (4 links) before the deep material; hero sentence untouched.

Note: D-2/D-3/D-4 were authored INLINE by the integrator — both Sonnet
subagent workers died on the weekly spend limit before writing anything
(clean trees). Content unchanged from the packet specs.

## Phase D-main — GATED

Blocked on the nightshift-functional-mvp exit demo (docs describe the
system you have, not the one the docs wish existed). D-5 operators/ layer
(nightshift page written against the LIVE lane), D-6 integrators/ (work-
container v1 is ratified `74dcf86`), D-7 reference/ consolidation, D-8
research-annex migration with stubs + mapping table, D-9 Fable coherence +
stranger-read + external-URL passes.
