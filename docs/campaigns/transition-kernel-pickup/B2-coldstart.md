# B2 cold-start — invariant survival map

**Teed up 2026-07-03 (do NOT start warm at a session tail — this is the handle).**
Executor: **Fable** (conceptual seam; per model-tier routing). Tier: conceptual.
Resume COLD from the receipts below, not from a warm summary.

## What B2 is

The **invariant survival map**: the table of Lean-backed distinctions that must
survive the Lean→Rust transition-kernel port. It is analysis + a table, not code.
One row per invariant:

| invariant | Lean source (module · theorem · tier) | Rust surface | corpus case | refusal mode |

Output goes into `INVENTORY.md` §"Invariant survival map" (new section). The
commit cites the `~/git/lean` HEAD it read.

## Prereqs — ALL MET (with receipts)

- **B1 done** (ground truth): INVENTORY.md §"Three worlds, 2026-07-02" — the
  shared vocabulary is the 12-kind refusal enum + 5 seams; the corpus is the
  contract; the packet's wicket-verdict map was REFUTED. Do not reinterpret B1.
- **Corpus at 13** (B5 quartet + freshness_subcase): `golden/corpus/` 01–13; AG
  sovereign + `~/git/transition-kernel` mirror byte-identical; differential 13/13
  (both kernels reproduce). This is the `corpus case` column's source.
- **Slice 1b adopted + verified on main** (B4, receipt `59cf2553`): the AG
  standing grant-use adapter is live — relevant to the Standing/LA/kernel/receipts
  separation row.

## Inputs a fresh session needs (where the columns come from)

1. **Lean source column** → `~/git/lean` (sweep on kernel-axis work; see the
   `lean_admissibility_kernel` memory). Each row cites module + theorem + **tier**
   using the citation-tier discipline: `[1.0]` citable / `ANNEX` exact-theorem /
   `SCRATCH` pilot-only / `CANDIDATE` uncitable. ANNEX cannot ratify.
2. **Rust surface column** → what the transition-kernel Rust types/verdicts/
   receipts/refusals must express. `~/git/transition-kernel` (the mirror +
   `scripts/differential.py`).
3. **corpus case column** → `golden/corpus/` (13 cases). Rows lacking a corpus
   case **enumerate into B5** (one missing case = one B5 slice).
4. **refusal mode column** → the closed 12-kind refusal enum (B1 ground truth).
   B2 may NAME missing refusals; it adds none.

## The invariant rows to fill (enumerated in the B2 purpose)

Start the table from these (from NEXT.md §B2):
- typed refusals **naming the offender** (v5/v6)
- metric-time freshness (4 refusals + skew/divergence params)
- finitary exhaustively-matched verdict enums
- corrective = down-edge
- `checkpoint_mints_nothing`
- one-receipt-one-discharge
- the Standing / LA / kernel / receipts separation
- citation tiers themselves ([1.0]/ANNEX/SCRATCH/CANDIDATE)

## Stop condition

An invariant statable only with NEW kernel vocabulary → mark `requires_operator`
and file in `DECISIONS.md` (do not axiomatize the convenient answer; exhibit
model-dependence per the CorrectiveBoundary methodological reference if a warrant
is ambiguous).

## Downstream unblocked by B2

- **B3** corpus plan (Q-B3 already resolved: AG-sovereign + mirror).
- **B5..Bn** corpus expansion — every B2 row without a differential case becomes
  one mechanical slice (`B5-work-order.md` tracks the 4 already named, blocked on
  operator/prior-gap).
- **B6/B7** (review + framing) prereq [B2].

## Not B2's job

Slice 1b (done), corpus custody (Packet C done), the 4 parked B5 drills (blocked
elsewhere). B2 is the MAP; it builds nothing.
