# Roadmap — spine × AG

**Status:** DRAFT (2026-07-02; ratifiable from exploration evidence)
Repo: `~/git/spine` (HEAD `16ef81f`, 2026-06-28; charter fixed, implementation
early) · Docket: governor-atlas constellation case · Memory: spine = constellation
READ plane (indexes, doesn't bear status)

## 1. Contract snapshot — what AG assumes today

- No runtime dependency either direction. Spine depends on Continuity
  (semantic substrate); AG is corpus, not consumer.
- Load-bearing invariant AG cites: **"findability is not legitimacy"** — indexing
  a thing does not make it true. The canonical predicate-witness note lives in AG
  (`docs/cross-tool/predicate-witness-infrastructure-note.md`); spine mirrors it.
- Spine slices to date: doctrine index, the Edition ("freeze navigation, not
  authority"), corpus expansion, Declaration Source Interface v0, edition compare
  ("describe drift, never succession"), continuity-shaped fixture ("the envelope,
  never the verdict").

## 2. Observed drift (dated)

None. Spine 2c remains PARKED-adjacent by its own record; not blocking AG.

## 3. Named gaps (non-binding)

- `READ_PLANE_BOUNDARY_NOTE` — spine (constellation-wide reading),
  governor-atlas (AG-architecture claim graph), and AG's `state_index_export.v0`
  (AG-repo state projection) are three legibility surfaces growing toward each
  other. Their boundaries should be written before they collide.
  (= CONSOLIDATION.md candidate #5; likely verdict "distinct scopes, write it
  down" — but that's C2's call.)

## 4. Slices

### R-SPINE-1 — read-plane boundary note
tier: conceptual · executor: fable · prereq: [C2 adjudication of candidate #5]
- purpose: one page in docs/cross-tool/ naming what each of the three read-planes indexes, what none of them may testify to (status/authority), and the citation direction between them.
- files: docs/cross-tool/read-plane-boundaries.md (new).
- tests: n/a (doctrine note).
- refusal mode: n/a.
- receipt shape: commit citing C2's evidence rows.
- stop condition: if C2 recommends absorption instead — this slice dissolves into that verdict's work.

## 5. Do-not-build

- No AG feed into spine ahead of spine's own Declaration Source Interface
  maturing (spine pulls; AG does not push).
- No status/authority fields in anything spine-facing — read plane stays read.

## 6. Operator questions

None beyond consolidation candidate #5 (C2).
