# Demo: allow all tools, emit receipts
[gateway]
agent_id = "demo-agent"
receipt_path = "./receipts/demo_allow.jsonl"

[upstream]
command = ["python", "demos/echo_server.py"]

[policy]
default_policy = "allow"
deny_tools = []
