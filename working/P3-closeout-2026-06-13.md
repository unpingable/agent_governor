# P3 closeout + pre-P4 alignment — 2026-06-13

Item 1 of the pre-P4 order (0 stale-note fix ✓ · **1 closeout/alignment** · 2 prep-
before-ingest · 3 P4 plan · 4 start P4). Records the P3 spine truth and the
prior-work alignment sweep that reconciled older doctrine with the now-built spine.
**Still pre-P4: closing the plan-intake/decomposition gap before promotion/expiry.**

## What is now REAL

| Slice | Commit | What it is |
|---|---|---|
| P3.0 / P3.0b | — | activation preflight; **DebtLedger = authoritative live claim source** |
| P3.1 + drill | 5c88dfc / aa5e00b | scoped four-office activation + rollback, one tunable; lifecycle drilled |
| P3.2 | 52e797f | enforcing RecompositionReceipt — `refused_laundering` blocks |
| pre-P4 doctrine | 2428072 / dd7a9a0 / ec99cbf | decomposition-closure, symbolic-witness, hotpath notes |
| conversion audit | a4bc70f | 0 blocker / 0 live-risk over 11 conversions |
| P3.3 | 77044f2 | decomposition-completeness receipt shape (schema truth) |

## Still BOOTSTRAP / degraded (documented, accepted — not bugs)

- **In-process custody/evidence forgeability** — P3.1 (forge+put+apply) and P3.3
  (evidence objects) fence the SHAPE, not provenance. Genuine provenance is a later
  custody-anchoring rung. Same ruling both times.
- **Standing = operator-fiat stub** — standalone may not mint its own standing; the
  honest substitute is the operator's nod, typed non-convertible.
- **Spend/custody = local** — local exactly-once ledger + local receipt store.
- **External Standing/LA/NQ NOT claimed** — constellation mode refuses to fake rich
  (`REFUSED_DEGRADED_CLAIMS_BACKING`).

## Explicitly NOT solved (named, not built)

- Capability kernel / grant ledger (the closure half of decomposition).
- Verifier (Z3) producing real `SolverCoverageEvidence`; Lean cited-not-run.
- Seam-cap composition rule (`A;B` needs explicit seam evidence).
- Discharge→activation collector binding (`GOV_GAP_DISCHARGE_COLLECTOR_BINDING_001`).
- Prep-before-ingest indecomposable-gate blocker (P3.4, next).
- Receipt-kernel ratification-invariant supersession; Rust extraction.

## What gates P4

P4 = **constitutional memory** (a trial shape surviving → becoming baseline), NOT
more annealing. Preconditions: P3 drill ✓, P3.2 enforcement green ✓, DebtLedger ✓,
P3.1 activation receipts ✓, promotion target = the one P3.1 tunable, supersession
ceremony chosen, baseline diff/revert proven — **plus** the pre-P4 gate **P3.4**
(plan-intake admission fenced). *Before P4 can promote anything, AG needs a front
door that can refuse plans whose gates are not yet decomposable.*

## Alignment sweep results (independent agent, classified)

3 DANGEROUS_STALE · 3 NEEDS_WORDING_UPDATE · 1 NEEDS_FUTURE_GAP · 4 CONSISTENT.
**All actionable items fixed in this pass:**

- **loop-protocol §11.3** (highest leverage — live procedure): "record future debt as
  named debt (plan or commit body)" → **mint a NonDischargeClaim in the DebtLedger**
  (binds collector, blocks rung). The fuse line too. *Named is not collected; carried
  is not collected either.* Added: classification authority sits above the rung.
- **campaign card §7 slice tracker** — was frozen at "P1.1 IN PROGRESS 2026-06-12 /
  rest OPEN" while the body said P3.1–P3.3 LANDED. Rewritten to truth; `P3a/P3b`
  naming reconciled to **P3.1/P3.2**, decomposition work = **P3.3**, prep-before-
  ingest = **P3.4**. P4 preconditions block added.
- **gap status headers** — `RUNG_DEBT_COLLECTION_001` ("not yet built") and
  `DECOMPOSITION_COMPLETENESS_..._001` ("NOT a build") flipped to LANDED-with-residual
  (their own bodies already said LANDED).
- **feature-history.md** — added the Workflow-Kernel/Self-Annealing Spine entry
  (was grep-count 0 despite being shipped 3.x-platform code).
- **NEEDS_FUTURE_GAP** — filed `GOV_GAP_DISCHARGE_COLLECTOR_BINDING_001` (the
  discharge hot path: a claim becoming non-blocking is consequence-bearing).

CONSISTENT (verified not-stale): four-office note's constellation mode is explicitly
*future*; `annealing_and_recomposition.md` correctly forbids checkpoint→auto-baseline;
parked-p31 standing stub is correctly bootstrap. No external-office-wiring or
auto-promotion contradictions found.

## Next

**P3.4 prep-before-ingest indecomposable-gate blocker — LANDED** (`prep_ingest.py`):
ingest refuses while an `indecomposable_gate` claim is open; one operator-gated
discharge socket (structured ref, no generic flag-flip); plan_id+gate_id namespaced;
fail-closed reader. Pre-P4 order is now `0 ✓ · 1 ✓ · 2 ✓ · 3 P4 plan update · 4 start
P4`. **Next: item 3 — the P4 promotion/expiry plan update** (constitutional memory: a
trial shape surviving → becoming baseline), then start P4. P4 preconditions are all met
now (see the campaign card's P4 block); the pre-P4 plan-intake gate (P3.4) is fenced.
