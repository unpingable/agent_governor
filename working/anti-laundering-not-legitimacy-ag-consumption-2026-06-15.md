# Anti-laundering, not legitimacy — AG consumption note (2026-06-15)

Source: `~/git/papers/working/governance-kernel-scope-correction.md` (papers-side FROZEN
doctrine, papers-claude's; per the independence firewall AG **consumes**, does not
duplicate or co-author). This is the AG-side implication map only.

## The scope correction (consumed, not re-authored)
> **The kernel is not a legitimacy engine. It is an anti-laundering engine for declared
> orders.** It cannot abolish arbitrary power; it can force arbitrary power to **sign its
> own name** — pool unlicensedness at named roots where blame/audit/refusal/succession
> attach.

The FENCE (do not let any AG artifact violate): root trace ≠ legitimacy; root abuse is
*visible, not cured*; no legitimacy-engine reading (power made **attributable**, never
**legitimate**). Composes with `no-free-standing-bridge-schema` (mechanism) and the
no-lift family (`working/lift-failure-family-2026-06-14.md`).

## AG implication 1 — P4.0b (the HIGH gate, tomorrow): attributable ≠ legitimate
The promotion ceremony via operator basis makes the baseline mint **attributable** (signed
by the named operator-root), NOT **legitimate** (it does not make the trial good or the
promotion correct). Guard the P4.0b narrative against legitimacy-engine creep: the
`PromotionReceipt` + operator basis force the act to *bear the operator's name*; they do
not certify the decision was right. This is the scope-level statement of
`best-effort-completeness`'s round-3 lesson (**operator is not a universal solvent** —
operator basis licenses the *authority* gap, not the evidentiary/custodial ones). Both say:
the operator can SIGN, not LAUNDER. Pin into the P4 HIGH-prep design.

## AG implication 2 — the synchronic / diachronic architecture seam
The note names a real seam AG already half-lives:
- **Synchronic (token-local):** "does *this* receipt discharge its kind here-and-now?" —
  the P4 producer gates, the evidence walk, `decomposition_completeness`. AG's gates.
- **Diachronic (trajectory / Δt):** forum-shopping and laundering-over-time are *patterns
  over sequences* — the **deadlock detector** (`src/governor/deadlock.py`), and any future
  laundering-over-time check. Watches the loop window, not a single token.

> Do not fold a diachronic pattern into a synchronic token check, and do not let a
> synchronic pass claim diachronic cleanliness.

This is *why* the deadlock detector is correctly a separate sequence-layer, not a gate; and
it's theorem surfaces #6 (`synchronic admissibility ⇏ diachronic cleanliness`) and #7
(`locally licensed crossings can launder over time`) — which ARE the no-lift family's
temporal/trajectory rows. The papers note is the **purpose** behind the schema AG keeps
re-deriving.

## AG implication 3 — `JurisdictionOf` is not magic
"Named, contestable, refusable — never an oracle that silently confers standing" is AG's
admissibility posture already (scope governor; the multigov "contested jurisdiction →
deadlock → operator-required" path). A contested jurisdiction must refuse token-local
discharge, not silently pick a basin. Confirms the deadlock/multigov custody design.

## Status / non-action
Papers-side doctrine is frozen; the 7 candidate theorem surfaces are UNRATIFIED /
name-early / not-build. AG does not build against them and does not promote this into AG
doctrine (would duplicate papers authority + risk the firewall). Recorded as a consumption
handle: the fence governs AG's self-description (AG is anti-laundering, not legitimacy —
composes with [[project_antiagentic_thesis]]) and the P4.0b narrative. No code, no
ratification.
