# Candidate — the laundering braid (cross-surface)

Status: **candidate, for a fresh session. Do NOT promote/overbraid now.** Filed
2026-06-10. Name early, ratify lazily.

## The observation

Two pieces of doctrine landed this session turned out to be the **same disease on
different surfaces**:

- **Language ratchet** (`docs/reference/constellation-lexicon.md`): *refusal is an
  evidentiary boundary operation, not punitive enforcement.* → bad language can launder
  bad posture.
- **Two-verdict ratchet** (`working/campaign-tick-tock-builder-ratchet.md` § "The builder
  loop"): *"the patch worked" must not launder "the process was unsafe."* → passing cargo
  can launder unsafe process.

Same shape:

```
bad language        launders   bad posture
passing cargo       launders   unsafe process
successful execution launders  broken custody
```

Provisional unifying line (NOT yet ratified):

> Operator-facing vocabulary and builder-loop verdicts must preserve **distinct
> validation surfaces** — a success on one surface must not be readable as a success on
> another.

## Guard (from ChatGPT, kept verbatim in spirit)

> That's the doctrine-adjacent braid. **Don't overbraid it into a scarf.**

The two plans should **cross-reference lightly, not merge.** This is a recognition
record, not a new kernel. Resist the urge to build a "laundering theory" with arrows on
every surface (same failure shape as the dependency-skeleton trap).

## Why deferred

Both source surfaces are already shipped and clean (language ratchet: Slices 0–2,
pushed; two-verdict ratchet: commit `2b8eca3`, in use). The braid adds no execution
work — it's a possible *doctrine* observation. Whether it earns a ratified home (a
sentence in CLAUDE.md, or a one-line cross-ref between the lexicon and the campaign card)
is a fresh-session call, decided only if a third surface exhibits the same
launder-across-surfaces shape (doctrine-promotion rule: candidate until recurrence).

## If picked up in a fresh session

Smallest viable move first: a one-line mutual cross-reference between
`constellation-lexicon.md` and the campaign card's builder-loop section, naming the
shared "distinct validation surfaces" invariant. Nothing larger unless a third instance
forces it.
