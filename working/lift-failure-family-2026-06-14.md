# The "no-lift" family — umbrella recognition (2026-06-14)

**Candidate / cross-project promotion candidate. NOT promoted, no version bump, no
register entry.** Dogfoods `feedback_ratification_vs_progress_deadlock`: record the
provisional recognition; don't promote on momentum. Pinned here because the four
*instances* are each recorded but the recognition that they are ONE pattern was not.

## The umbrella
> **Local validity does not automatically lift across a composition, consumer,
> trajectory, or custody boundary.**

The reusable negative pattern. Local correctness is necessary, never sufficient for the
global property — and the boundary it fails to cross is the load-bearing variable.

## The family — WITH STATUS (the fence that keeps the clean table honest)
It is a **local-to-global obstruction** family (sheaf-flavored): a recognized
mathematical shape (legible/citable, not crank) made of NEW instances (operator custody /
refusal-as-architecture aren't in the sheaf literature — not derivative either). But
"recognized family" must not let the table cosplay as a result it hasn't earned.

```
axis        local ⇏ global                          status
--------    -----------------------------------     -----------------------------------
Observer    artifact stamp ⇏ consumer-indep verdict LITERAL global-section theorem
                                                     (HasGlobalSection ↔ ConsumersAgree)
Temporal    hopwise admissible ⇏ trajectory adm.    schema-aligned, NOT reduced
Deadlock    locally valid deferrals ⇏ global prog.  schema-aligned; compiled specimen
                                                     valid_deferrals_can_deadlock + AG detector
Custody     plural observations ⇏ plural custody    schema-aligned; finite-arithmetic specimen
  (QuorumCustody)                                    (quorum overlap + honest node +
                                                     honest-no-double-certify ⇒ no conflicting
                                                     certificates) — custody safety, NOT "BFT solved"
Reflexive   (NoFreeStandingBridge applied to self)  schema-aligned — see Lean-side record
```
Canonical record (source of truth, 5-axis + per-row descent template): the Lean-Claude's
`no-free-standing-bridge-schema.md` (lean repo, scratch). This AG note is the AG-side
capture; defer the full table + the per-row "five descent questions" to it.

### Two guards (load-bearing, not vibes)
1. **Meta-bridge guard — "fits the family" ≠ "reduces to a member."** Only the Observer
   row is a literal global-section theorem (the robe applied). The other four are
   formally-verified *no-lift specimens* that are schema-aligned, not reduced. Claiming
   they're sheaf corollaries without building the translation would be **a free-standing
   bridge smuggled into the meta-theory of NoFreeStandingBridge** — the on-brand-but-bad
   move, refused explicitly. Each row earns its upgrade by running the descent questions,
   the way Observer did.
2. **Correlated-convergence guard — agreement ≠ proof.** Two Claudes + ChatGPT converging
   on "schema" is common-mode, not evidence ([[feedback_claude_independence_firewall]] /
   common-mode synthesis, applied to itself). Observer is the artifact that breaks the
   tie; the rest are conjectured rhymes. The status column exists so the clean table
   can't pass itself off as a withhold-shaped consensus.

Why it still earns "family": each instance generated the *same* runtime discipline
independently — **checked ≠ typed**, **producer-derived ≠ producer-trusted**,
**eligible ≠ authorized**, **recommend-the-process ≠ decide-the-content**. The shared
discipline is the umbrella's fingerprint — strong enough to name the schema, NOT strong
enough to claim four reductions. One cell is global-section-shaped; four await translation.

## Day's final state (operator, 2026-06-14)
```
Temporal packet:     closed scratch packet
Observer packet:     generic Verdict nucleus in scratch (noun provisional: VerdictFor)
Deadlock primitive:  AG governor detector LANDED (pure, unwired); Lean model = Lean-Claude's
QuorumCustody:       scratch custody-safety theorem, Mathlib footprint justified
Version:             no bump (scratch indexed/committed ≠ release surface)
Promotion:           none
```

## If it graduates
Promote under **Custody Safety / Quorum Custody Annex** — never "BFT consensus." The
abstract keeps the mine-shaft sign in neon: these are *negative* lift theorems (local ⇏
global), not solved-distributed-systems claims. Graduation = a second independent
recurrence + operator ratification, not "we found four in one day" (the four are one
session's family — strong, but one firing of the *umbrella* recognition).

## AG-side stake
The umbrella is why AG's primitives compose the way they do: every gate this campaign
refuses a *lift* (assertion→authority, trial→baseline, eligible→promoted, observation→
custody). If the umbrella ever ratifies, AG's refusal vocabulary is its largest worked
example. Until then: recognized, not load-bearing.
```
